/**
 * This files contains environment specific identifiers
 * that enable environment detection from within the code
 */

/**
 * Default environment identifiers
 */
export const environment = {
  /**
   * Whether or not the app is running in production mode
   */
  production: false,
  /**
   * Whether the app is run within Electron. false for web services
   */
  isElectron: false
};
